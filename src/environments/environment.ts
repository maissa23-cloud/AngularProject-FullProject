export const environment = {
  production: false,
  apiBaseUrl: 'https://localhost:7001/api'
};

// Enable local development authentication mock. Set to false in production.
export const devAuth = {
  enable: true
};
